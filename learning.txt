Frontend will run on http://localhost:3000 (CRA default).
Backend will run on http://localhost:3001 (from .env).
The frontend dev server will proxy API requests to http://localhost:3001 and CORS will allow requests from 3000 and 3001.